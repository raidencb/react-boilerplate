import React from 'react';
import ReactDOM from 'react-dom';
import styles from './index.module.scss';

ReactDOM.render(
  <h1 className={styles.headings}>Hello World</h1>,
  document.getElementById('root')
);
